name: dblp-nli
services:
  frontend:
    build:
      context: ./dblp-nli-app
      args:
        API_URL: http://backend:9000
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      - backend

  backend:
    build: .
