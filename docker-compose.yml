services:
  frontend:
    build: ./dblp-nli-app
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://backend:5000
    depends_on:
      - backend

  backend:
    build: .
    ports:
      - "5000:5000"